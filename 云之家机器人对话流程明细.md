# 云之家机器人对话流程明细

## 一、消息接收流程

当用户@机器人时，云之家会根据开发者配置的 HTTPS 服务地址，把消息内容发送出去。

---

## 二、请求协议

### 2.1 HTTP 请求示例

**HTTP HEADER：**
```json
{
  "Content-Type": "application/json; charset=utf-8",
  "sign": "xxxxxxxxxx",
  "sessionId": "XXXXXX"
}
```

**HTTP BODY：**
```json
{
    "type": 2,
    "robotId": "xxxx",
    "robotName": "提醒机器人",
    "operatorOpenid": "xxxx",
    "operatorName": "小明同学",
    "time": 1599727083000,
    "msgId": "xxx",
    "content": "多喝热水,身体健康"
}
```

### 2.2 请求 Header 参数说明

| 参数 | 说明 |
|------|------|
| sign | 签名值，用于验证请求合法性 |
| sessionId | 会话 ID，用于标识上下文 |

### 2.3 请求 Body 参数说明

| 参数 | 类型 | 说明 | 是否必填 |
|------|------|------|----------|
| type | int | 消息类型，文本消息为 2，消息卡片为 25 | 是 |
| robotId | String | 加密的机器人 ID | 是 |
| robotName | String | 机器人名称 | 是 |
| operatorOpenid | String | 加密的发送者 ID | 是 |
| operatorName | String | 发送者名称 | 是 |
| msgId | String | 加密的消息 ID | 是 |
| content | String | 消息内容 | 是 |
| time | long | 当前时间戳（毫秒） | 是 |

---

## 三、签名验证（sign）

### 3.1 签名作用

用于判定请求是否来自云之家的合法请求，避免其他仿冒者。

### 3.2 计算方法

1. 将参数中的摘要信息（robotId、robotName、operatorOpenid、operatorName、time、msgId、content）用逗号连接
2. 与机器人的 appSecret 一起作为签名字符串
3. 使用 **HmacSHA256** 算法计算签名
4. 进行 Base64 编码，得到最终签名值

### 3.3 代码示例（Java）

```java
package util;

import org.apache.commons.codec.binary.Base64;
import org.apache.commons.lang3.StringUtils;

import javax.crypto.Mac;
import javax.crypto.spec.SecretKeySpec;
import java.security.InvalidKeyException;
import java.security.NoSuchAlgorithmException;
import java.util.Date;

public class SignUtil {

    private static final String HMAC_SHA256_ALGORITHM = "HmacSHA256";

    public static String generateSign(String secret, String robotId, String robotName, 
                                      String operatorOpenid, String operatorName, 
                                      String time, String msgId, String content) 
                                      throws NoSuchAlgorithmException, InvalidKeyException {
        // 构建摘要信息
        String summaryInfo = StringUtils.join(
            new String[]{robotId, robotName, operatorOpenid, operatorName, time, msgId, content}, 
            ","
        );
        
        // 计算 HMAC-SHA256
        SecretKeySpec signKey = new SecretKeySpec(secret.getBytes(), HMAC_SHA256_ALGORITHM);
        Mac mac = Mac.getInstance(HMAC_SHA256_ALGORITHM);
        mac.init(signKey);
        byte[] signData = mac.doFinal(summaryInfo.getBytes());
        
        // Base64 编码
        return new String(Base64.encodeBase64(signData));
    }
}
```

---

## 四、会话管理（SessionID）

### 4.1 作用

- 维护对话上下文语境
- 避免多个用户同时与机器人对话时产生混淆

### 4.2 使用规则

- 相同 sessionId 表示同一用户在同一群组与同一机器人的多次对话
- **会话有效期：30分钟**

---

## 五、响应格式

### 5.1 响应示例

```json
{
    "success": true,
    "data": {
        "type": 2,
        "content": "好风凭借力，送我上青云"
    }
}
```

### 5.2 响应参数说明

| 参数 | 类型 | 说明 | 是否必填 |
|------|------|------|----------|
| success | boolean | 请求是否成功 | 是 |
| data.type | int | 消息类型，文本为 2，卡片为 25 | 是 |
| data.content | String | 返回的消息内容 | 是 |

---

## 六、接入常见问题

| 错误描述 | 原因 |
|----------|------|
| 请求响应状态码错误 | 发起对消息接受地址的请求失败 |
| 请求响应时间超过3秒 | 响应超时 |
| 请求响应结果格式不符合文档要求 | 返回数据格式错误 |

---

## 七、机器人测试

### 7.1 测试请求

云之家会主动对公网可访问的本企业 HTTPS 服务地址，用于接收 POST 过来的消息，之后将对应的响应信息封装为结果展示给用户。
发起测试请求验证地址正确性：


**测试 HTTP HEADER：**
```json
{
  "Content-Type": "application/json; charset=utf-8",
  "sign": "jy/WTAtltv5UVQVDOb0f4H4JPqw=",
  "sessionId": "XXXXXX"
}
```

**测试 HTTP BODY：**
```json
{
    "type": 2,
    "robotId": "test-robotId",
    "robotName": "test-robotName",
    "operatorOpenid": "test-userId",
    "operatorName": "test-userName",
    "time": 1599727083000,
    "msgId": "test-msgId",
    "content": "你好，你能做什么呢?"
}
```

### 7.2 测试参数说明

| 参数 | 测试值 |
|------|--------|
| robotId | test-robotId |
| robotName | test-robotName |
| operatorOpenid | test-userId |
| operatorName | test-userName |
| msgId | test-msgId |
| content | 你好，你能做什么呢? |
| secret | test-secret |

### 7.3 注意事项

- **响应超时时间：3秒以内**
- 超时返回用户提示：`哎呀,出现了点小问题,要不等会再试试？`

---

## 八、密钥获取机制

### 8.1 创建流程

机器人创建流程需经过以下步骤：

1. **配置消息接收地址**：开发者在云之家后台配置 HTTPS 服务地址
2. **触发测试请求**：云之家自动向配置的地址发起测试请求（详见第七章）
3. **验证测试响应**：云之家验证响应是否符合规范（格式正确、响应时间 < 3秒）
4. **返回密钥**：测试通过后，系统才会创建机器人并返回密钥（appSecret）

### 8.2 密钥用途

获取的 `appSecret` 用于后续所有请求的签名验证：

- 开发者使用 `appSecret` 计算签名（sign）
- 通过对比请求头中的 sign 与自行计算的签名，验证请求合法性
- 测试阶段使用临时密钥 `test-secret`，正式环境使用返回的真实密钥

### 8.3 重要提示

- **测试未通过**：若测试请求响应不符合要求，机器人创建失败，不会返回密钥
- **密钥安全**：`appSecret` 是验证请求合法性的关键，需妥善保管，避免泄露
- **重新测试**：若测试失败，修正问题后需重新提交创建请求，系统会再次发起测试

---

## 九、注意事项汇总

1. 必须验证签名（sign）确保请求合法性
2. 响应时间必须在 3 秒以内
3. 返回数据格式必须符合规范
4. 消息类型：文本为 2，消息卡片为 25
5. 机器人密钥仅在测试通过后才会生成并返回