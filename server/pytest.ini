[pytest]
# 云之家机器人后台（server）测试配置。
# 无参运行 `pytest`（本目录下）即收集 tests/ 全部用例，无需指定路径或猜测测试根目录。
#
# 注意：本服务顶层包名为 `app`，与 proxy-server/ 相同，两者无法被同一个 pytest 进程
# 同时导入。运行全部测试请见仓库根目录 README 的「运行测试」小节。
testpaths = tests
addopts = -ra
